---
name: add-script
description: Add a PEP 723 script under scripts/ for {{name}}
---

# Add script

1. Create `scripts/<name>.py` with PEP 723 metadata.
2. Run with `uv run scripts/<name>.py`.
3. Add a smoke test under `tests/`.
