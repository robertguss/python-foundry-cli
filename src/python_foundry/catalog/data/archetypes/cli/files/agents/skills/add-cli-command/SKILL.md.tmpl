---
name: add-cli-command
description: Add a CLI command to {{name}}
---

# Add CLI command

1. Add a function in `src/{{module}}/cli.py` or a new module.
2. Wire it from the entrypoint.
3. Add tests under `tests/`.
4. Run quality gates.
