---
name: secrets-fnox
description: Secrets via fnox + age for {{name}} (no dotenv secret storage)
---

# Secrets (fnox + age)

Use:

```bash
fnox exec -- uv run <entry>
```

Do **not** store secrets in `.env` / python-dotenv / committed env files.
