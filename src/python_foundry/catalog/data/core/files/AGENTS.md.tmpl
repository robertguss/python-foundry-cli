# {{name}}

Generated by python-foundry.

## Quality gates

```bash
uv sync
uv run ruff check .
uv run ty check
uv run pytest
```

## Secrets

Use `fnox exec -- …` for secret-consuming commands. Do not store secrets in dotenv.
