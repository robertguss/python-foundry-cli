# Editor documentation (REQ-077)

## Supported

- VS Code / Cursor: open Generated Project; use `uv run` tasks; no required MCP kitchen sink.
- Agent hosts: follow Generated `AGENTS.md` + `.agents/skills/*` only.

## Not in v1 Core emit

- Claude Code adapters (`CLAUDE.md`, `.claude/`)
- Default committed MCP catalogs
